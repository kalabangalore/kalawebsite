# Copy to server/.env for local development.
# On Vercel, set these as Project → Settings → Environment Variables instead.

# Neon Postgres connection string (use the pooled "-pooler" host on Vercel)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Local API port (ignored on Vercel)
PORT=4000

# Admin login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
JWT_SECRET=use-a-long-random-string

# Google Apps Script Web App URL that saves payment receipt uploads to
# Drive and logs a row in Sheets — see scripts/payment-receipt-apps-script.gs.
# Receipt uploads are skipped (logged, not fatal) if this isn't set.
APPS_SCRIPT_URL=
